{
  "name": "pracear-admin",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "tsc -p tsconfig.json && tsc -p admin/tsconfig.json"
  },
  "devDependencies": {
    "typescript": "^5.4.5"
  }
}
